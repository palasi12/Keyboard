{
  "name": "taptile-render-rig",
  "private": true,
  "description": "Turntable frame renderer for the landing page scroll sequence.",
  "scripts": {
    "setup": "npm i && node -e \"require('fs').copyFileSync('node_modules/three/build/three.min.js','three.min.js')\" && npx playwright install chromium",
    "preview": "node capture.js --preview",
    "render": "node capture.js"
  },
  "dependencies": {
    "playwright": "^1.47.0",
    "three": "0.128.0"
  }
}
